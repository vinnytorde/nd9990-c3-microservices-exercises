# deployment

apiVersion: apps/v1
kind: Deployment
metadata:
  labels:
    deployment: udagram-api-feed
  name: udagram-api-feed-deployment
spec:
  selector:
    matchLabels:
      app: udagram-api-feed
  replicas: 2
  template:
    metadata:
      labels:
        app: udagram-api-feed
    spec:
      containers:
      - image: vinnytorde/udagram-api-feed:v1
        name: udagram-api-feed
        imagePullPolicy: Always          
        resources:
          requests:
            memory: "64Mi"
            cpu: "250m"
          limits:
            memory: "1024Mi"
            cpu: "500m"       
        ports:
        - containerPort: 8080
      restartPolicy: Always

---
# service

apiVersion: v1
kind: Service
metadata:
  labels:
    service: udagram-api-feed
  name: udagram-api-feed-svc
spec:
  ports:
  - name: "8080"
    port: 8080
    targetPort: 8080 
  selector:
    app: udagram-api-feed